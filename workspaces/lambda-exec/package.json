{
  "name": "lambda-exec",
  "version": "0.0.0",
  "private": true,
  "main": "./dist",
  "files": [
    "./dist"
  ],
  "scripts": {
    "prepublish": "tsc -p ."
  },
  "dependencies": {
    "esbuild": "^0.8.26",
    "execa": "^5.0.0",
    "tmp-promise": "^3.0.2"
  }
}
